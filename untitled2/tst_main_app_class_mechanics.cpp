#include <gmock/gmock-matchers.h>
#include <gtest/gtest.h>
#include "vehicle.h"
#include "carbook.h"
#include "carevent.h"

using namespace testing;
/*
TEST(UnitTest_Classes, Creating_Values)
{

    EXPECT_EQ(1, 1);
    ASSERT_THAT(0, Eq(0));
}

TEST(VehicleTest, InitialState){
    Vehicle *vehicle = new Vehicle();
   // EXPECT_EQ(vehicle, IS_NOT_EQ) << "Initial default vehicle name should be 'Generic'";
    EXPECT_EQ(vehicle, nullptr);
}
*/
